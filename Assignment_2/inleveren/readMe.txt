In all file extensions, .txt should be removed

To succesfully run profit_table.py, both Input_processing.py and transformExcel.py should be run

transformExcel.py uses an old package to transform the given excel file to a csv for demand, and make an .h5 file for fleet information, if you have problems running this, we're happy to share these files (these are not uploaded since it was explicitly stated not to upload the input data, and because binary files (.h5) are not allowed.